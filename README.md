# Linux Command Interpreter (Mini Shell)

A custom Linux shell built in C with job control.

## Features
- Command execution (ls, pwd, etc.)
- Background processes (&)
- Job control (jobs, fg)
- Signal handling (SIGCHLD)
- Built-in commands (cd, exit)

## How to Run
gcc shell.c -o myshell
./myshell
